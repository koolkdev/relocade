[package]
name = "wasm86-x86"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
wasm86-compiler = { path = "../compiler" }

[dev-dependencies]
wasmparser = { version = "=0.224.1", default-features = false, features = ["std", "validate"] }
